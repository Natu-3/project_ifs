# AWS RDS endpoint 
DB_HOST=mydb.xxxxxxxx.ap-northeast-2.rds.amazonaws.com
DB_PORT=3306
DB_NAME=ifscm
DB_USER=admin
DB_PASSWORD=change-me

# Use immutable tags from CI if possible (sha-xxxx)
BACKEND_IMAGE=backwork:latest
FRONTEND_IMAGE=reactwork:latest

# For production safety, avoid auto schema mutation
JPA_DDL_AUTO=validate
SPRING_PROFILES_ACTIVE=prod